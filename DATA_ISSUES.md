# DATA_ISSUES — مشاكل الداتا والنتائج المترتبة عليها

> الوثيقة تُحدّث عند فحص داتا تب `all` (أكتوبر 2026 — 227 صف).
> الداشبورد **لا يعدّل** الداتا الأصلية أبدًا؛ كل ما يلي يُعرض داخل قسم **جودة البيانات**.

---

## 1) عمود `Column 19` غير معروف

- **الأعمدة المتأثرة**: `Column 19` (العمود 20 في تب all).
- **المشكلة**: اسم العمود التلقائي من Google Forms ولم يُعبأ بأي قيمة حتى الآن.
- **لماذا مشكلة**: لا يمكن تحديد معناها الوظيفي من البيانات (0 قيمة).
- **التأثير**: غير مؤثر حاليًا على أي تحليل (فارغ تمامًا)، لكنه يبقى "غير معروف" في Data Dictionary.
- **الحل المقترح**: عند ظهور أول قيمة، يُراجع المعنى؛ وفي انتظار ذلك يُعرض عمودًا "غير معروف" ولا يُستخدم في أي KPI.

## 2) أعمدة "التاريخ" المكررة/المتشابهة بالاسم

- **الأعمدة**: التاريخ (5)، تاريخ خروجها (7)، التاريخ 2 (10)، تاريخ عملها (14)، التاريخ 3 (22).
- **المشكلة**: أسماء متشابهة لكنها تُستخدم في سياقات مختلفة (مصروف / فاتورة / توريد / مرتجع)، ولا توجد تسمية موحدة.
- **لماذا مشكلة**: سهولة الخلط عند بناء Mapping يدوي.
- **التأثير**: تمت معالجته في الداشبورد عبر تعيين كل عمود لنوع الفورم الخاص به فقط (فترة صلاحية لكل عمود).
- **الحل المقترح**: توحيد الأسماء إلى `تاريخ المصروف`، `تاريخ خروج الفاتورة`، `تاريخ التوريد`، `تاريخ عمل الفاتورة`، `تاريخ المرتجع` في رأس الـ Sheet أو في طبقة الـ Mapping.

## 3) تنسيق أحد أعمدة الخصم `Discount` غير منتظم

- **الأعمدة**: `Discount` (18).
- **العينات**: `No`, `1.5٪`, `2.5٪`, `90 جنيه`, `10جنيه`, `2جنيه`, `4جنيه`, `جنيه`, `68.0`.
- **لماذا مشكلة**: مزيج بين "نسبة مئوية" و"مبلغ ثابت" و"لا خصم" ونصوص مفتوحة؛ لا يمكن حساب "إجمالي الخصم" رقميًا بشكل قاطع لكل الحالات.
- **التأثير**: نحسب إجمالي/متوسط الخصم **النقدي فقط** (قيم `جنيه` وحيدة) ونعرض النسب المئوية كأرقام منفصلة؛ وهناك صفوف تُحتسب "بخصم غير محدد".
- **الحل المقترح**: إضافة عمود `نوع الخصم` (نسبة/مبلغ/لا) أو اعتماد تنسيق واحد، ولو كان `0` بدل `No`.

## 4) قيمة شاذة في المحافظة: `و`

- **الأعمدة**: `المحافظه` (21).
- **العينة**: فاتورة المحافظة = `و` (صف واحد).
- **لماذا مشكلة**: ليست محافظة صحيحة — إدخال غير مكتمل.
- **التأثير**: تظهر كقيمة منفصلة في تصنيف المحافظات.
- **الحل المقترح**: إعادة تعبئة الصف من المصدر. تعرض من قبل الداشبورد تحت "قيم مشبوهة".

## 5) تهجئات متغيرة لنفس المحافظة

- **الأمثلة**: `المنوفيه` مقابل `المنوفية`، `الجيزه الهرم المريوطيه` مقابل `الجيزه المريوطيه`، `اسيوط` مقابل `اسيوط ديروط`، `القاهره السلام`.
- **لماذا مشكلة**: تُقسّم المحافظة الواحدة لمجموعات متعددة في الرسوم البيانية.
- **التأثير**: دقة متوسطة في تصنيف المحافظات.
- **الحل المقترح**: قائمة ترميز (Aliases) تُطبَّق على المستوى العرضي فقط بدون تعديل المصدر.

## 6) `رقم الفاتوره` قيم تبدو كأرقام تسلسلية تشبه التواريخ

- **العينات**: `45961`, `46045`, `46036`, `45226` ...
- **لماذا مشكلة**: عند تحويل Excel serial تبدو كتواريخ (2025-11) رغم أنها "رقم فاتورة".
- **التأثير**: لا يمكن الجزم هل رقم الفاتورة هو تاريخ فعلي أم رقم متسلسل.
- **الحل المقترح**: عرض الرقم كما هو، وعدم تفسيره كتاريخ. توثيق القرار في README.

## 7) مسافات زائدة وغير موحدة

- **الأعمدة**: `النوع` (3)، `الطريقه` (9)، `صاحب الفاتوره` (15)، `phone number` (19)، `المحافظه` (21).
- **الأمثلة**: `فودافون كاش␣`, `012 27945370`, `0 10 07930158`, `مواصلات داخل تلا␣`.
- **التأثير**: تكرارات وهمية في التصنيفات.
- **الحل المقترح**: `trim + normalize` في طبقة التطبيع الداخلية فقط.

## 8) عمود الصور فارغ

- **الأعمدة**: `صوره` (6)، `صوره الفاتوره` (16).
- **الحالة**: لا توجد روابط صور حاليًا.
- **التأثير**: لا صور في الجداول. الداشبورد جاهز (Thumbnail + Preview + Lazy) لو ظهرت لاحقًا.

## 9) التوثيق فارغ

- **الأعمدة**: `توثيق` (11)، `توثيق 2` (27).
- **الحالة**: لا توجد قيمة في كل الصفوف الحالية (الفواتير والمرتجعات).
- **التأثير**: يعرض قسم البيانات الجودة "فواتير بدون توثيق = كل الفواتير"، وبالتالي لا يُعتمد عليه كمؤشر نشط حتى يُعبَّأ.

## 10) صفوف فارغة آخر تب `all`

- **الملاحظة**: الصفوف من 229 حتى 321 معرفة في الـ Sheet لكنها فارغة.
- **التأثير**: يتجاهلها الداشبورد نهائيًا (فلاتر `isEmpty`).

## 11) نطاق تواريخ متقاطع غير منضبط

- **العينات**: يوجد توريد بتاريخ `46294` (2026-09-29) بينما آخر فاتورة تاريخ عمل `46228` — والفواتير تسبق تواريخ توريدات أجدد.
- **لماذا مشكلة**: تؤثر على أي "مقارنة فترات" دقيقة.
- **الحل المقترح**: الفلاتر تعمل على "تاريخ العملية حسب نوعها"، ويُوثَّق النطاق في التقرير.